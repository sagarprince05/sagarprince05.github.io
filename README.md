# Prince Sagar — Portfolio

A fast, accessible portfolio for a Forward Deployed Engineer — dark "midnight" palette (navy, blue glows, mint accent, aurora gradient blocks), photo-led (hero with a cut-out portrait, serif-italic title, tech marquee, screenshot collage, "how it works" cards, results, selected works, contact). Hand-coded in HTML, CSS and vanilla JavaScript — no build step, no frameworks, no trackers.

## Files

```
index.html                       Home: hero, marquee, collage, about, process, results, work, background, contact
projects/
  site-onboarding.html           Case study — Site Onboarding & Approval Automation
  humyn-watcher.html             Case study — Humyn Watcher (AI Slack assistant)
  voucher-price-tracker.html     Case study — Voucher Price Tracker
styles.css                       Design tokens (:root) + all layout + case-study styles
script.js                        Mobile nav, active nav, scroll reveal, copy email, lightbox
assets/
  favicon.svg                    "P" mark — change the letter/colours here
  prince.webp                    Portrait cut-out (generated by tools/make_photo.py)
  resume.pdf                     Résumé (linked from the Experience section)
  og.png                         1200×630 social preview image (regenerate: see below)
  screenshots/                   WebP screenshots + card thumbnails (generated by tools/make_screenshots.py)
```

## Run locally

Open `index.html` in a browser, or serve the folder:

```bash
python -m http.server 4173
```

then visit http://localhost:4173.

## Editing

| What | Where |
|---|---|
| Name, title, blurb, photo | `index.html` → `<section class="hero">` (photo: `assets/prince.webp`; remove the `filter: grayscale(1)` line in `styles.css` for colour) |
| About statement + chips | `index.html` → `<section id="about">` |
| Experience / education / skills | `index.html` → `<section id="background">` |
| Project cards | `index.html` → `<section id="work">` — duplicate a `.work-card` |
| Case studies | `projects/*.html` — copy one to add a new case study, then link it from a card |
| Contact email | `index.html` → `mailto:` link, `data-email`, and the JSON-LD block in `<head>` |
| Colours | `styles.css` → `--accent` (mint), `--blue`, `--aurora` and the surface tokens in `:root` |

To use a real screenshot instead of a CSS mock-up on a work card, replace the `<div class="mock">…</div>` with `<img src="assets/screenshots/….webp" alt="" width="640" height="400" loading="lazy">`.

## Deploy (free)

**GitHub Pages (recommended — matches the URLs already in the `<head>`)**
1. Create a repo named `sagarprince05.github.io` and push this folder to `main`.
2. Settings → Pages → Source: `main`, root folder.
3. Live at https://sagarprince05.github.io within a minute.

**Netlify / Vercel** — drag-and-drop the folder, no build command, publish directory `.`. Then update `og:url` and `canonical` in every HTML file.

## Before going live

- [x] Add `assets/resume.pdf`
- [x] Add `assets/og.png` (1200×630)
- [x] Real screenshots for Site Onboarding and Humyn Watcher (`python tools/make_screenshots.py` regenerates `assets/screenshots/`)
- [ ] Run Lighthouse in Chrome DevTools (Performance / Accessibility / Best Practices / SEO)

## Regenerating images

```bash
python tools/make_photo.py        # portrait cut-out from the original photo
python tools/make_screenshots.py  # WebP screenshots + card thumbnails
python tools/make_og.py           # 1200×630 social preview (uses the cut-out)
```

Edit the name, title, tagline or chips inside `tools/make_og.py` and re-run.
